
# Minimal .htaccess for Node.js Application
# The Node.js entry point is handled by the hosting environment.
# We just ensure DirectoryIndex doesn't interfere.

DirectoryIndex disabled
RewriteEngine On
Options -MultiViews
